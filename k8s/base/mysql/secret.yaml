apiVersion: v1
kind: Secret
metadata:
  name: mysql-credentials
  annotations:
    # PreSync の migration Job より先に Secret を適用する
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-2"
# サンプル用の平文パスワードであり本番利用禁止
stringData:
  password: password
  root-password: rootpassword
